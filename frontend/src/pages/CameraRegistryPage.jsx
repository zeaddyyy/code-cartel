export default function CameraRegistryPage({ cameras, online, PageHeading, PanelHeader }) { return <div className="page-view"><PageHeading title="Camera registry" subtitle="Registry, stream metadata, and connection health" count={`${cameras.length} cameras`} /><section className="panel registry-panel"><PanelHeader title="Camera registry" action={`${online} online`} /><div className="table-wrap"><table><thead><tr><th>Camera</th><th>Location</th><th>Stream</th><th>Health</th><th>Last seen</th></tr></thead><tbody>{cameras.map((camera) => <tr key={camera.id}><td><b>{camera.camera_id}</b><small>{camera.name || "Unnamed"}<br />{camera.id}</small></td><td>{camera.location_name || "Unavailable"}</td><td>{camera.stream_codec || "Unknown"} {camera.stream_width ? `${camera.stream_width}×${camera.stream_height}` : ""}</td><td><span className="table-status">{camera.health_status || camera.status || "UNKNOWN"}</span></td><td>{camera.health_last_seen ? new Date(camera.health_last_seen).toLocaleString() : "Not reported"}</td></tr>)}</tbody></table>{!cameras.length && <div className="empty-state">No cameras returned by the backend.</div>}</div></section></div>; }
