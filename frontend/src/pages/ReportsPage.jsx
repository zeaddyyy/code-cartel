import { useState } from "react";
export default function ReportsPage({ PageHeading, PanelHeader }) { const [message, setMessage] = useState(""); const reports = ["Daily Camera Report", "AI Detection Report", "Vehicle Movement Report", "ANPR Report", "Alert Report", "Camera Health Report"]; const request = (type) => setMessage(`${type} export is not available yet; configure the reporting endpoint before presenting an export as successful.`); return <div className="page-view"><PageHeading title="Operational reports" subtitle="Generate review-ready outputs from authoritative platform data" count="6 report types" /><section className="panel registry-panel"><PanelHeader title="Report builder" action="Export boundary" /><div className="report-controls"><input type="date" aria-label="Report start date" /><input type="date" aria-label="Report end date" /><select aria-label="Report camera"><option>All cameras</option></select><button onClick={() => request("Report")}>Preview</button></div><div className="report-grid">{reports.map((report) => <article className="report-card" key={report}><span>REPORT</span><h2>{report}</h2><p>Uses stored platform records with explicit source and time range.</p><button onClick={() => request(report)}>Export CSV</button><button onClick={() => request(`${report} PDF`)}>Export PDF</button></article>)}</div>{message && <p className="error">{message}</p>}</section></div>; }
