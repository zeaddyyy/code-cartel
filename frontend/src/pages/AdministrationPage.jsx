export default function AdministrationPage({ cameras, cameraForm, updateForm, addCamera, editCamera, deleteCamera, PanelHeader }) { return <div className="page-view"><div className="page-heading"><div><small>OPERATIONS / ADMINISTRATION</small><h1>Camera administration</h1><p>Register, update, and remove cameras from the NetraX registry.</p></div><span className="page-badge">{cameras.length} registered</span></div><section className="panel admin-panel"><PanelHeader title="Register camera" action="PostgreSQL registry" /><form onSubmit={addCamera} className="admin-form">{["camera_id", "name", "location_name", "latitude", "longitude"].map((field) => <input key={field} required={field !== "name" && field !== "location_name"} placeholder={field.replace("_", " ")} value={cameraForm[field]} onChange={updateForm(field)} />)}<button type="submit">Register camera</button></form></section><section className="panel registry-panel"><PanelHeader title="Registered cameras" action="CRUD" /><div className="table-wrap"><table><thead><tr><th>Camera ID</th><th>Name</th><th>Location</th><th>Status</th><th>Actions</th></tr></thead><tbody>{cameras.map((camera) => <tr key={camera.id}><td><b>{camera.camera_id}</b><small>{camera.id}</small></td><td>{camera.name || "Unnamed"}</td><td>{camera.location_name || "Unavailable"}</td><td><span className="table-status">{camera.health_status || camera.status || "UNKNOWN"}</span></td><td><button className="table-action" onClick={() => editCamera(camera)}>Rename</button><button className="table-action danger" onClick={() => deleteCamera(camera.id)}>Delete</button></td></tr>)}</tbody></table>{cameras.length === 0 && <div className="empty-state">No cameras are registered.</div>}</div></section></div>; }
