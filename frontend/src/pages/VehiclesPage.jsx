export default function VehiclesPage({ vehicles, EvidenceImage, PageHeading, PanelHeader, DataTable }) {
  return <div className="page-view"><PageHeading title="Vehicle intelligence" subtitle="Vehicle detections, evidence snapshots, and camera-scoped tracks" count={`${vehicles.length} events`} /><section className="panel registry-panel"><PanelHeader title="Vehicle sightings" action="Evidence-aware" /><DataTable rows={vehicles} columns={["Vehicle", "Plate", "Camera", "Track", "Evidence", "Time"]} render={(detection) => <><td><b>{detection.object_class}</b><small>{(Number(detection.confidence) * 100).toFixed(1)}% confidence</small></td><td>{detection.plate_number || "Unavailable"}</td><td>{detection.camera_name || detection.camera_id}</td><td>{detection.track_id || "Untracked"}</td><td><EvidenceImage snapshotPath={detection.snapshot_path} alt="Vehicle evidence" /></td><td>{new Date(detection.detected_at).toLocaleString()}</td></>} empty="No vehicle detections returned." /></section></div>;
}
